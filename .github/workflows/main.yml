name: ScoutPulse CI/CD

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.21'

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Build Apps
        run: |
          echo "Building services..."
          # Future commands:
          # cd apps/identity-svc && go build ./...
          # cd apps/frontend && npm install && npm run build

      - name: Run Tests
        run: |
          echo "Running tests..."
          # Future commands:
          # go test ./...
